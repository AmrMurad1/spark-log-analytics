name: Databricks Community Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4 
      - name: Setup Databricks CLI
        uses: databricks/setup-cli@main

      - name: Manual Sync to Workspace
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          # We explicitly call workspace import-dir. 
          # This avoids the 'bundle' logic and the SCIM error.
          databricks workspace import-dir ./LogInsights /Users/amrabdelrahman7788ss@gmail.com/LogInsights --overwrite